<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :room_location, style: "display: block" %>
    <%= form.text_field :room_location %>
  </div>

  <div>
    <%= form.label :room_capacity, style: "display: block" %>
    <%= form.number_field :room_capacity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
